@model IndexViewModel

@{
    ViewBag.Title = "Food Market";
    ViewBag.Description = "We sell stuff!";
    ViewBag.Keywords = "food grain rice";
    //ViewData.Add("Title", "Food Market");
    //ViewData.Add("Description", "We sell stuff!");
    //ViewData.Add("Keywords", "food grain rice");
    var base_path = Context.Request.PathBase;
}
@*<h2>Home Page!</h2>

<img src="~/content/static/polina-rytova-1dGMs4hhcVA-unsplash.jpg" width="500"/>*@
<div class="main-img">
    <span class="title">Food Market</span>
</div>

<div class="container">
    @*<ul>
            @foreach (var post in Model)
           {
               <li>
                   <a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">
                       @post.Title
                   </a>
               </li>
           }
        </ul>*@

    <partial name="_Pagination" model="Model"></partial>

    @foreach (var post in Model.Items)
    {
        var image_path = $"{base_path}/Image/{post.Image}";
        <a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id" class="post">
            <img src="@image_path" width="500" />
            <span class="title">
                @post.Title
            </span>
        </a>
    }

    <partial name="_Pagination" model="Model"></partial>

</div>